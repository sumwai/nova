# 变更记录

记录 nova 对外的变更。版本号规则见 README 的「配置语法代数」与「版本」两节：
程序版本用 `x.y.z`（兼容变更 `z+1`、不兼容变更 `y+1` 且 `z=0`、大功能变更 `x+1` 且 `y=0`、`z=0`），
不兼容的配置语法变更同时把配置语法代数 `+1`。

## Unreleased

**Gemini 协议**

- 新增线协议 `gemini`（Google Generative Language `generateContent`）：可作为上游渠道，
  也可作为客户端协议。端点地址用 `{model}` 指代模型名（要加引号，否则 `{` 会被当成块开启），
  网关按本次请求是否流式把末段在 `:generateContent` 与 `:streamGenerateContent?alt=sse`
  之间互换，凭据以 `x-goog-api-key` 注入。客户端路径为
  `/v1beta/models/<模型>:generateContent` 与 `:streamGenerateContent`，`models/` 前缀之后作为对外名。
- 协议转换覆盖 system 提示上提（`systemInstruction`）、`model` 角色、`functionCall` /
  `functionResponse` 片段、`inlineData` / `fileData` 图片、`toolConfig.functionCallingConfig`
  的 AUTO / ANY / NONE 映射，以及 `thought` 推理片段与 `usageMetadata` 的两侧口径换算；
  函数参数的 JSON Schema 按 Gemini 的 OpenAPI 子集裁剪（丢掉 `additionalProperties`、`$schema`
  一类上游会拒的字段），流式工具调用的 `partialArgs` 按 JSONPath 拼回完整参数。
- 上游地址可由适配器按本次请求构造：新增可选能力 `domain.UpstreamURLBuilder`，
  模型名与动作不在请求体里的协议（Gemini）因此不必把地址写成动态模板。
- 入口层新增可选能力 `domain.RequestBinder`：路径携带模型名与流式标记的协议在解码前
  把路径事实绑定到请求级副本上，单例适配器不被写入。
- Gemini 端点不支持 `discover`：清单响应形状不同，且带占位符的地址推不出清单地址。

**跨渠道分摊与回退**

- 新增顶层 `route <对外名模式> { ... }` 块与块内的 `provider` / `fallback`：
  同一个对外模型名落在多条渠道上时，用它声明候选与调度规则。块头含 `*` / `?` 时
  匹配一组对外名；匹配大小写敏感，多条规则按声明顺序取第一条命中。
- 候选按三段固定顺序拼成一条链：`provider` 行（主用，写 `balance` 时按权重轮转起点）→
  `fallback` 行（显式回退，不参与轮转）→ 规则没提到的渠道（按声明顺序兜底）。
  「主用全部失败才切换」因此是结构上的事实。
- `provider` / `fallback` 一行可列多个渠道名（各权重 1）；权重写在它所属渠道名之后，
  因此渠道名不能是纯数字。
- 同一个渠道的多条端点候选作为一个整体移动：先试完这条渠道的全部端点，再换下一条。
- 命中规则的模型不再做「同协议候选优先」的隐式排序：规则写下的顺序就是最终顺序。
- 一条规则没有命中任何对外名、或规则里的某个渠道没有提供匹配该模式的模型，
  装配时各记一条提醒；空规则、模式含空白、渠道重复、块头重复、引用不存在的渠道、
  权重不是正整数、渠道名是纯数字均报错。

**一个渠道多个账号**

- `api_key` 可写多条，一条一个账号：它们共用本块的地址、协议、超时与模型声明，
  只有凭据不同。第二取值是可选的分摊权重（`api_key {env.K} 3`，省略即 1）。
- 新增 `balance`：把账号池从「按声明顺序」（缺省，前面的失败才换后面的）改为
  「按权重轮询分摊」。每次请求把起点前移一个位置，链尾仍保留全部账号，
  因此分摊在正常情况下生效、故障时照常回退。起点计数器是渠道级的，
  同一渠道下的不同模型共享一条轮转序列。
- 账号池文件：`import` 在 provider 块内同样生效，被导入的文件就是一段 provider 块体，
  因此账号池可以单独放一个文件（或按 glob 拆成多个）。池文件走同一套词法、注释、
  `{env.NAME}` 与 `文件:行:列` 报错；glob 内按文件名字典序拼接，文件名决定账号顺序。
- 写了权重却没写 `balance`（权重没有作用）、单账号写了 `balance`（没有可分摊的对象），
  各记一条提醒；权重不是正整数、`balance` 带取值、`balance` 写两次均报错。
- 上游尝试记录与日志带账号引用（`acct #2`），主行的 `via` 同样带它；熔断与失败折叠
  按「渠道 + 账号」分开计：一个账号被限流不影响同渠道的其它账号。单账号渠道的账号引用
  为空串，日志与折叠键与多账号之前逐字一致。
- `config check` 与启动横幅在多账号时输出一行账号池摘要（账号数与调度口径）。

**模型自动发现**

- 新增 `discover [<地址>]`：端点的模型集合从上游清单接口取。省略地址时从端点 `url` 推导——
  裁掉协议端点段再拼 `/models`，版本根原样保留，因此 `/v1/chat/completions`、
  `/v1/messages`、`/api/coding/paas/v4/chat/completions` 都推出各自版本根下的清单地址。
- 新增 `allow` / `deny`：按通配模式过滤发现来的模型。模式匹配整个 id、两端隐式锚定，
  `*` 可跨 `/`，大小写不敏感，`deny` 优先；显式声明不受过滤，且与发现项同名时胜出
  （可借它改写上游名）。一条过滤规则都不写时记一条提醒。
- 新增 `expose <上游模式> <对外名模式>`：把清单里的上游 id 按模式改写成对外名（加前缀、
  去前缀、换前缀、去后缀）。上游模式必须含恰好一个 `*` 作捕获，两边都不收 `?`；
  可写多条、第一条命中即生效；过滤仍按上游 id 判断、改名在过滤之后；改名是替换，
  原名不再对客户端存在；两条清单项改名后撞同一个对外名报错，一条规则没命中任何清单项时
  记一条提醒。不依赖清单的精确改名仍用 `model <对外名> <上游名>`（上游漏项时它仍生效，
  也是发现失败时降级集合的来源）。
- 发现只在启动与 reload 时执行，多端点并发，与转发共用同一个 HTTP 客户端与同一套
  请求头规则（凭据加协议内置头）。失败时：端点有显式模型就退回它们并记 `error`，
  没有显式模型则装配失败；reload 失败不换装配、在途请求不打断。
- 清单响应自动识别形状：OpenAI 的 `object` / `created` / `owned_by` 与 Anthropic 的
  `type` / `display_name` / `created_at` 统一归一化；`has_more` 为真时记一条 `warn`，
  本版不翻页。响应体上限 4 MiB，超时不超端点自身的 `timeout`。
- 新增数据面 `GET /v1/models`：形状按 `anthropic-version` 头区分（带该头回答 Anthropic 形状，
  其余回答 OpenAI 形状），与其它路径一样过 `client_key`；只接受 `GET`，忽略分页参数，
  固定回全量。它只读当前目录快照，不触发发现。
- 新增 `nova models [-c PATH] [--provider <名>]`：装配一次目录，按端点列出显式声明的模型
  （含别名映射）、清单保留与排除的模型 id，再加一行目录汇总；保留项在改名后显示
  「对外名 → 上游名」，未命中的改名规则单独列一段。`--provider` 只看一条渠道（汇总也只算它，
  名字不存在时报错），`--verbose` 给保留项附上 `static` / `discovered` 来源标注。
  它要连上游，因此与 `config check` 分开；发现过程的日志不另写 stderr，避免与报告重复。
- `nova config check` 校验新指令并提示发现型端点的清单未经校验（它仍然不连上游）。
- 启动横幅新增目录大小与发现统计（清单条目、保留、排除、降级端点数）；每条端点的发现
  另记一条 `catalog` 记录（json 里是 `model_discovery`），行上直接列出客户端可用的对外名
  （`expose` 改名后的结果）：文本模式最多列 20 个并给出总数，json 模式给全量。

**日志**

- 发现记录的名单补上显式声明的对外名（text 里是单独的 `显式：…` 一段，json 里是新增的
  `declared` 字段）：只列清单那一侧时，配下的 `model` 行看起来像没生效，而它其实是
  客户端可用的名字之一。
- 接上上游尝试记录与访问日志。text 模式以**一次请求一个块**排版：access 主行在前，
  同一请求的上游明细在后，块内各行共用主行的时刻与列宽。失败时上游的 HTTP 状态码与
  响应片段因此不再丢失。
- 上游明细只写主行没有的事实：上游 id、两侧协议与模型的改写、上游用量、上游报文。
  主行已承载的客户端状态码、耗时与错误码不重抄——单次尝试时它们与主行是同一个事实，
  而入口耗时与上游耗时并不严格相等，重抄会让同一个名字出现两个数。
- 唯一一次成功的上游尝试折进主行（用量与上游 id 随之上移），因此常见的单次成功请求
  只占一行；候选回退、跨协议改写、上游报文才展开成明细行。
- 明细行的级别取自身级别与主行级别中的较高者；客户端取消仍记 `debug`，不被主行抬起来。
- 同一类上游失败（同一条渠道上的同一个错误码）在 10 秒窗口内只留首条全文，其余折叠计数，
  窗口结束时补一条 `×N` 汇总：上游持续限流时逐字相同的报文会连刷几十行，
  真正的信号「上游一直在限流」反而被淹没。
- text 模式的关联键只留前 8 个字符（截断值是完整值的前缀，仍可用前缀 grep 命中），
  消息名列固定宽度，因此不同记录种类的字段从同一列开始。
- 新增 `log_format` 配置指令：`text`（缺省，给人读）与 `json`（给机器读）。
  两者不是同一记录的两种排版：text 省略「常见情况下不提供信息」的字段，不可逆；
  json 保留每个请求按 `request_id` 关联的 `access` 与 `upstream_attempt` 两条记录，不折叠。
- 启动横幅也走日志通路，在 json 模式下是一条带完整字段的 `startup` 记录。
  它不受 `log_level` 过滤：横幅回答的是「这个进程在用哪份配置跑」。
- 着色不进配置：输出落在终端、未设 `NO_COLOR`、`TERM` 不是 `dumb` 时才给级别与错误码着色。
- 上游标识从 `provider 名 + 完整地址` 改为 `provider 名 + 主机名`：scheme、版本根与
  端点路径对「分辨是哪条渠道」没帮助，却占了日志行很大一截。同主机不同协议的端点
  由同一条记录里的协议字段区分。

**仍未实现**

- 可观测性的采集侧：没有指标（metrics）与追踪（traces）导出。
- 清单的定时刷新（`discover_interval`）：目录只在启动与 reload 时更新。
- 清单分页：`has_more` 为真只记一条提醒，可能漏掉上游后续页里的模型。

## v0.1.0

**配置**

- Novafile 新增上游渠道声明：`provider` 块、`endpoint` 子块与 `model` 映射，
  同一对外模型名跨端点按声明顺序回退。
- 新增 `client_key`（客户端鉴权，可多条）、`import`（拆分渠道文件）、`{env.NAME}` 占位符。
- 线协议省略时从上游地址末段推导，写了则必须与推导结果一致。
- 缺省监听地址从 `:8080` 收紧为 `127.0.0.1:8080`：数据面没有鉴权时只绑回环，
  绑到非回环地址会记一条警告。

**转发**

- 接入转发链路：按对外模型名选路、同协议透传与跨协议重建、候选回退、流式与非流式。
- 客户端鉴权由 `client_key` 驱动，接受 `Authorization: Bearer` 与 `x-api-key` 两种形态，
  失败时按客户端协议编码 401；`/healthz` 不经鉴权，以免上游出问题时探活连带失败。
- 上游凭据按 provider 注入，凭据头的形态由该端点的线协议决定。
- 未注册路径回 404，路径与协议名的对应关系不可配置。

**修复**

- `make build-binary` 注入的版本号改为最近的 git tag，不再带 git describe 的
  `-N-g<hash>` 后缀：那个字段既不是可比较的版本号，也不是提交号。

**已知限制**

- 可观测性尚未接入：上游尝试记录与访问日志都还没有实现，`log_level` 目前只影响
  启动横幅与 reload 提醒，因此一次失败的转发在日志里看不到上游的状态码。
- `reload` 不接受 `listen` 与 `admin` 的改动，改这两项需要重启 nova。
- 管理端点没有鉴权，能连上它的人就能改写运行中的配置，因此缺省只绑回环。

## v0.0.0

首个版本：只有程序本体，转发链路尚未实现。

**命令行**

- 新增 `nova run`：装配配置，监听数据面与管理端点。
- 新增 `nova reload`：按配置里的 `admin` 地址热重载运行中的网关，监听地址不变、在途请求不打断。
- 新增 `nova config check`：只校验配置、不启动服务，适合放进 CI 或提交前的钩子。
- 新增 `nova version [--json]`：报出版本号、提交、支持的配置语法代数与全部指令名。
- 全局标志 `-h` / `--help` / `-?` 与 `-v` / `--version`；退出码 `0` 成功、`1` 运行期失败、`2` 用法错误。

**配置**

- 新增 Novafile 语法：全局指令直接写在顶层，一行一条；`#` 到行尾是注释；取值含空白时用双引号。
- 新增 `version` 指令声明配置语法代数。代数落在本二进制支持区间之外时，加载在解析任何指令之前失败，
  并报出「这份配置声明了什么、本二进制支持什么」，而不是先撞上某句「未知指令」。
- 新增 `log_level`（`debug` / `info` / `warn` / `error`）、`listen`、`admin` 三条指令。
- 未声明 `version` 时按本二进制实现的最高代数解析，并记一条提醒。
- 未知指令按编辑距离给出最接近的合法指令，候选过远时省略建议。
- 同一指令写两次即报错，并指出前一次所在行，而不是让后一次静默覆盖前一次。
- 所有错误携带 `文件:行:列`，列号按 rune 计数，含中文的行也能报出人眼可数的列。

**工程**

- `make check` 门禁：编译、`go vet`、`go test -race`、格式检查。
- 构建产物的唯一出口是 `bin/nova`，安装的唯一出口是 `$(PREFIX)/bin/nova`（缺省 `/usr/local/bin/nova`）。
- 版本号在构建期注入，取值顺序为 `VERSION=` > `git describe --tags --dirty` > `0.0.0`，
  含白名单外字符时构建直接失败，不做静默清洗。
- `mise.toml` 固定 Go 1.25.0。

**已知限制**

- 转发链路尚未实现：数据面只提供 `/healthz`，其余路径返回 501。
- `reload` 不接受 `listen` 与 `admin` 的改动，改这两项需要重启 nova。
- 数据面没有客户端鉴权。
- 管理端点没有鉴权，能连上它的人就能改写运行中的配置，因此缺省只绑回环。
